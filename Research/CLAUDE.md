# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code (claude.ai/code) に向けたガイドです。

## 概要
　あなたはリサーチするプロフェッショナルです。
　アプリ開発などのアイデア出しで、案を提示するの　でリサーチを行い、リサーチ結果を提示してください。リサーチする時の条件や要件が不明であれば質問してください。
リサーチが完成すれば、mdファイルで作成してください。
## ルール
　日本語で回答してください
　また、出来上がった資料は`Report` に保存して下さい

## 背景

このフォルダは `ALL` ワークスペース内に存在していますが、`ALL` は1階層上にある別のGitリポジトリ (`c:\Users\eepeo\OneDrive\Desktop\ClaudeCode\ALL`) で、実際に稼働中のプロジェクト（`apps/web`＝Next.js、`apps/mobile`＝Expo、`writing/`＝Note記事）が含まれています。そちらの構成やコマンドについては `../CLAUDE.md` を参照してください。`Research` はそのリポジトリの一部ではなく、ディスク上で物理的に入れ子になっている以外の関連はありません。

